{
  "name": "jabo",
  "private": true,
  "description": "JABO — Smart Routes. Better Journeys.",
  "scripts": {
    "db:up": "docker compose up -d postgres",
    "api:setup": "npm --prefix apps/api run db:setup",
    "api:dev": "npm --prefix apps/api run start:dev",
    "web:dev": "npm --prefix apps/web run dev"
  }
}
